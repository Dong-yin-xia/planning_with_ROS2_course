### 目录：test（测试样例）

**功能概述**
- 存放算法或第三方库的小型验证样例，目前包含 `osqp_test`（二次规划求解器示例）。

**子目录**
- `osqp_test/`: 演示如何链接 `OsqpEigen` 并运行简单优化问题（源码已提供头/源）。

**实现方式**
- 通过子目录 CMakeLists 组织目标；在根包 CMake 中加入 `add_subdirectory(src/test)` 以便构建。

**使用建议**
- 可根据课程/实验需要添加新的测试用例（如曲线平滑对比、坐标转换单元测试等）。


